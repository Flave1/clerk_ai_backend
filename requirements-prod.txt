# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.3.0  # Compatible with Python 3.12 and langsmith
pydantic-settings==2.1.0

# Real-time Communication
# livekit==1.0.13  # REMOVED: Used in livekit_bridge.py and telephony.py - comment back in if needed
# livekit-api==1.0.6  # REMOVED: Required by livekit
# livekit-protocol==1.0.7  # REMOVED: Required by livekit
websockets==12.0

# AI Services
openai>=1.6.1,<2.0.0  # Used for OpenAI API calls
# anthropic>=0.16.0,<1.0.0  # REMOVED: Required by langchain-anthropic - comment back in if needed
elevenlabs==0.2.26
# openai-whisper==20231117  # REMOVED: Very large (~1-2GB), use external service if needed
deepgram-sdk>=3.2.0  # Real-time STT with Deepgram Nova-2

# LangGraph and LangChain - REMOVED
# WARNING: This will break llm.py and summarization_service.py - code needs to be refactored
# langgraph==0.0.20  # REMOVED: Used in llm.py
# langchain==0.1.0  # REMOVED: Used in llm.py and summarization_service.py
# langchain-community==0.0.10  # REMOVED: Used in summarization_service.py
# langchain-openai==0.0.2  # REMOVED: Used in llm.py
# langchain-anthropic==0.1.0  # REMOVED: Not used anywhere in codebase
# langchain-core>=0.1.23,<0.2.0  # REMOVED: Used in llm.py

# AWS Services (Optional - for other AWS services)
boto3==1.34.0
botocore==1.34.0

# Database
motor==3.3.2  # Async MongoDB driver for Python
pymongo==4.6.1  # MongoDB Python driver

# HTTP & Async
httpx==0.25.2
aiohttp==3.9.1
# asyncio-mqtt==0.13.0  # REMOVED: Not used anywhere in codebase

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
email-validator==2.3.0  # Required for Pydantic EmailStr validation

# External Integrations
google-auth==2.25.2
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.2.0
google-api-python-client==2.110.0
slack-sdk==3.26.1

# Meeting Agent Dependencies
selenium==4.15.2  # Needed for Google Meet client automation
# webdriver-manager==4.0.1  # REMOVED: Not used anywhere in codebase
msal==1.24.1
apscheduler==3.10.4
docker==7.1.0  # Docker SDK for container management in scheduler

# Data Processing
# Only include if actually needed for production
numpy>=1.26,<2.0.0  # Used in stt.py for audio processing
# scipy==1.11.4  # REMOVED: Not used anywhere (~200-300MB)
# librosa==0.10.1  # REMOVED: Not used anywhere (~200-300MB)
# soundfile==0.12.1  # REMOVED: Not used anywhere (~50-100MB)
pydub>=0.25.1  # Audio processing (MP3 to PCM conversion) - used in tts.py

# Redis
redis==5.0.1
# hiredis==2.2.3  # REMOVED: Optional performance boost, not used in codebase

# Monitoring & Logging
# structlog==23.2.0  # REMOVED: Not used anywhere in codebase
# prometheus-client==0.19.0  # REMOVED: Not used anywhere in codebase

# Testing - REMOVED from production
# pytest==7.4.3
# pytest-asyncio==0.21.1
# pytest-mock==3.12.0
# moto[boto3]==4.2.14

# Development - REMOVED from production
# black==23.11.0
# isort==5.12.0
# flake8==6.1.0
# mypy==1.7.1

